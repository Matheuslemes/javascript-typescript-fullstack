// Tipos de dados primitivos

// string, number, undefined, null, boolean

const nome = "matheus" // string
const num01 = 10 // number
const num02 = 10.50 // number
let nomeAluno; // undefined -> não aponta para nenhum local na memória
const sobrenomeAluno = null; // Nulo -> não aponta para nenhum local na memória
const aprovado = true; // boolean, duas possibilidades de valores, valores lógicos

// tipo de dado passado por referência
const a = [1, 2];
const b = a;