// alert, confirm e prompt(navegador)

alert('Mensagem'); // atalho para window.alert. Retorna undefined

window.confirm('Deseja apagar?'); // retorna um valor booleano.

window.prompt('Digite o seu nome:') // pede ao usuario para digitar alguma informação.

const confirma = confirm('confirma?') // captura o valor do confirm passaod pelo usuário para a variável.