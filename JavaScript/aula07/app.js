// Constantes.

// não podemos criar constantes com palavras reservadas
// Constanes precisam ter nomes simplificados
// não podemos começar o nome de uma constante com u número
// não podem conter espaços ou traços
// utilizamos camelCase
// case-sensitive
// não pode modificar o valor de uma constante
// não utilize 'var', e sim 'const'

// operadores aritimeticos: +, -, *, /
// string = text | number = numero
const name = 'nome';

const primeiroNumero = 5; // number
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2;
const resultadoTriplicado = resultado * 3;

console.log(resultado);

// para saber o tipo do dado

console.log(typeof primeiroNumero);